import { Button } from '@fluentui/react-components';
import { Alert20Regular } from '@fluentui/react-icons';

const Notification = () => {
  return <Button size="small" appearance="transparent" icon={<Alert20Regular />} />;
};

export default Notification;
