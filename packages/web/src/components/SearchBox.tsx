import { SearchBox as FluentSearchBox } from '@fluentui/react-components';

const SearchBox = () => {
  return (
      <FluentSearchBox appearance='filled-lighter' placeholder='Search' />
  );
};

export default SearchBox;
